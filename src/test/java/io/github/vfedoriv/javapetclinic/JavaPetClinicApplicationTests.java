package io.github.vfedoriv.javapetclinic;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// note: this is a Spring Boot test class
@SpringBootTest
class JavaPetClinicApplicationTests
{
  @Test
  void contextLoads() {
        // this test will fail if the application context cannot be loaded
        // it is a simple test to check if the application can start up
        // and if all the beans are created successfully
        // no need to add any code here
        // just make sure that the application can start up without any errors
  }
}
